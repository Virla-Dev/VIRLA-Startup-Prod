{
  "rules": {
    "chats": {
      "$chatId": {
        ".read": "auth != null && data.child('members/' + auth.uid).exists()",
        ".write": "auth != null && (data.child('members/' + auth.uid).exists() || !data.exists())",

        "members": {
          ".write": "auth != null"
        },

        "messages": {
          ".indexOn": ["createdAt", "senderId", "receiverId"],

          "$messageId": {
            ".write": "auth != null && (
              (!data.exists() && newData.child('senderId').val() == auth.uid) ||
              (data.exists() && data.child('senderId').val() != auth.uid && newData.child('receiverId').val() == auth.uid &&
                data.child('content').val() == newData.child('content').val())
            )"
          }
        }
      }
    },

    "userChats": {
      "$userId": {
        ".read": "auth != null && auth.uid == $userId",
        "$chatId": {
          ".write": "auth != null && root.child('chats/' + $chatId + '/members/' + auth.uid).exists()"
        }
      }
    }
  }
}
